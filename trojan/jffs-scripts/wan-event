#!/bin/sh

# primary wan and connected
if [[ $1==0 && $2==connected ]]; then
  cru a run-iptables "*/3 * * * *" "apply_iptables"
  cru a run-v2ray "*/10 * * * *" "/opt/etc/init.d/trojan-forward start"
  cru a run-v2ray "*/10 * * * *" "/opt/etc/init.d/trojan-nat start"
fi
